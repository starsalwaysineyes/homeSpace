
### **应用需求文档 (PRD): HomeSpace 3D - 您的三维家庭物品索引**

**版本:** 1.0
**日期:** 2023年10月27日
**作者:** [您的名字/团队]

---

### **1. 项目概述 (Overview)**

#### **1.1 项目简介**
"HomeSpace 3D" 是一款个人家居环境三维建模与物品索引应用。用户可以通过简单的拖拽和设置，在虚拟的3D空间中快速搭建出自己家庭的布局（如房间、墙壁、家具），并将特定的家具或容器标记为“存储实体”。然后，用户可以向这些存储实体中录入物品信息（以文本列表形式）。当需要寻找某件物品时，只需通过全局搜索，应用即可快速定位到该物品所在的3D实体，并以高亮等方式直观地展示其位置。

#### **1.2 项目目标 (Goals)**
*   **解决核心痛点:** 解决“东西放哪了？”这个常见的家庭生活问题。
*   **降低使用门槛:** 提供极其简单的3D建模方式，无需专业知识，让普通用户也能快速上手。
*   **数据可视化:** 将物品的存储位置从抽象的记忆或表格，转变为直观、可交互的3D模型。
*   **高效检索:** 提供快速、准确的全局物品搜索功能。

#### **1.3 目标用户 (User Personas)**
*   **“健忘的整理者” (Forgetful Organizer):** 喜欢整理和收纳，但东西一多就容易忘记具体位置的用户。
*   **“搬家/装修规划者” (Mover/Renovator):** 需要规划新家物品布局，并预先记录放置位置的用户。
*   **“极客/效率追求者” (Geek/Efficiency Enthusiast):** 喜欢将生活数字化、条理化，追求高效生活方式的用户。
*   **“多物件收藏家” (Collector):** 拥有大量收藏品（如模型、书籍、电子元件），需要精细化管理其存储位置的用户。

---

### **2. 功能需求 (Functional Requirements)**

#### **2.1 3D场景与空间构建 (Scene & Space Construction)**
*   **FR-1.1: 基础场景:**
    *   应用启动后，提供一个带有网格参考线的空白3D“白板”或“地基”作为初始场景。
    *   用户可以定义这个基础场景的尺寸（例如，长10米 x 宽8米）。

*   **FR-1.2: 墙体/隔板创建:**
    *   提供一个“墙体工具”。用户可以通过在网格上“画线”的方式创建墙体。
    *   墙体具有可自定义的高度和厚度属性。
    *   支持墙体的拖动、拉伸和删除。

*   **FR-1.3: 基础实体创建:**
    *   提供一个包含基础几何体的工具箱。
    *   **V1.0 必须包含:** 长方体（可用于模拟柜子、桌子、床等）、球体、圆柱体。
    *   用户可以从工具箱中拖拽实体到3D场景中。

*   **FR-1.4: AI辅助实体创建:**
    *   **自然语言添加容器:** 用户可通过语音或文本输入"在客厅添加一个棕色衣柜"，系统自动在合适位置创建相应实体
    *   **智能位置推荐:** AI根据空间布局和现有家具，智能推荐新实体的最佳放置位置
    *   **自动尺寸调整:** 根据空间大小和用户描述，自动计算并设置合理的实体尺寸

*   **FR-1.5: 3D直接建模:**
    *   **空间构建方式:** 所有建模操作均在3D空间中直接进行，无需通过平面图转换
    *   **直观操作:** 用户可以直接在3D视图中进行拖拽、旋转、缩放等操作，提供直观的空间构建体验
    *   **实时渲染:** 前端负责所有3D渲染，后端仅存储元数据，确保流畅的交互体验

*   **FR-1.6: AI辅助空间修改:**
    *   **自然语言空间调整:** 支持类似"把客厅扩大一点"、"在卧室和客厅之间加一堵墙"的指令
    *   **智能布局优化:** AI根据空间利用率和用户习惯，提供布局优化建议
    *   **自动空间重构:** 根据用户需求，自动调整墙体位置、房间大小等空间结构

#### **2.2 实体操作与属性编辑 (Object Manipulation & Properties)**
*   **FR-2.1: 实体变换 (Transformation):**
    *   **移动 (Translate):** 用户可以沿X、Y、Z轴自由拖动场景中的任何实体（墙体、几何体）。
    *   **旋转 (Rotate):** 用户可以围绕实体的中心点进行旋转。
    *   **缩放 (Scale):** 用户可以等比或非等比地缩放实体，以调整其大小。

*   **FR-2.2: 属性编辑面板 (Properties Panel):**
    *   当用户选中一个实体时，界面上会显示其属性面板。
    *   **命名 (Naming):** 每个实体（包括墙体）都必须有一个可编辑的名称字段。此名称是后续识别的关键（例如：“客厅电视柜”、“主卧床头柜”）。
    *   **尺寸 (Dimensions):** 显示并可编辑实体的长、宽、高。
    *   **外观 (Appearance):** 允许用户为实体设置一个简单的颜色，以方便区分。
    *   **纹理支持:** 用户可上传照片作为实体纹理，增强真实感
    *   **纹理管理:** 提供纹理库功能，支持保存和复用常用纹理
    *   **自动适配:** 系统自动调整上传图片的尺寸和比例以适配实体表面

#### **2.3 存储功能核心 (Storage Core Functionality)**
*   **FR-3.1: 标记为存储实体:**
    *   在实体的属性面板中，提供一个“是否为存储容器？” (Is Storage Container?) 的复选框或开关。
    *   只有被标记的实体才能容纳物品。

*   **FR-3.2: 物品管理界面:**
    *   当用户双击或通过特定按钮点击一个“存储实体”时，弹出一个物品管理窗口/侧边栏。
    *   该界面显示一个物品列表（Item List），每个物品都是一个字符串。

*   **FR-3.3: 物品的增删改查 (CRUD for Items):**
    *   **添加 (Create):** 界面中有一个输入框和一个“添加”按钮，用户输入物品名称（字符串）后即可添加。支持批量添加（例如，用逗号分隔）。
    *   **读取 (Read):** 列表清晰地展示该存储实体内的所有物品。
    *   **更新 (Update):** 用户可以点击列表中的某个物品，对其字符串进行修改。
    *   **删除 (Delete):** 每个物品旁都有一个删除按钮。

*   **FR-3.4: AI赋能物品管理:**
    *   **自然语言添加:** 用户可通过语音或文本输入"在客厅抽屉里放一把钥匙"，系统自动识别并添加物品
    *   **自然语言移动:** 支持类似"把卧室的充电器移到书房"的指令，自动完成物品位置变更
    *   **自然语言删除:** 支持类似"删除过期的药品"的模糊指令，智能识别并删除相关物品

#### **2.4 搜索与定位 (Search & Locate)**
*   **FR-4.1: 全局搜索框:**
    *   应用主界面需要有一个常驻的、显眼的搜索框。
    *   搜索功能应支持模糊匹配（例如，搜索“证件”可以匹配到“身份证”）。

*   **FR-4.2: 搜索结果展示:**
    *   当用户输入关键词并搜索后，以列表形式展示所有匹配的物品。
    *   每条结果应包含：**物品名称** 和 **所在的存储实体名称**（例如：`[身份证] - 存于 [主卧床头柜第一个抽屉]`）。

*   **FR-4.3: 3D定位联动:**
    *   **这是核心功能！** 当用户点击某条搜索结果时，3D视图应自动发生变化：
        *   视角自动平移和缩放，聚焦到该物品所在的存储实体上。
        *   被定位到的存储实体应有明显的高亮、闪烁或线框效果，让用户一目了然。

#### **2.5 项目/场景管理 (Project Management)**
*   **FR-5.1: 保存与加载:**
    *   **本地存储:** 用户可以将其创建的整个家庭3D模型及其中存储的所有物品数据保存为单个项目文件（`.hsd` JSON格式）
    *   **云端同步:** 支持将项目数据保存到后端服务器，实现多设备同步
    *   **导入导出:** 支持从本地打开之前保存的项目文件，也支持导出为JSON格式
    *   **自动保存:** 应用支持自动保存到本地缓存和后端服务器

---

### **3. 非功能性需求 (Non-Functional Requirements)**

*   **NF-1: 性能 (Performance):**
    *   在包含约200个实体和总计5000个物品项的场景中，应用操作（移动、旋转、搜索）应保持流畅，无明显卡顿。
    *   应用启动时间应在5秒以内。

*   **NF-2: 易用性 (Usability):**
    *   界面设计应简洁直观，核心功能（添加实体、标记存储、搜索）应在最容易触及的位置。
    *   3D导航操作应符合主流习惯（例如，鼠标右键拖拽旋转视角，滚轮缩放，中键平移）。
    *   提供新手引导 (Onboarding Tutorial) 帮助用户快速了解基本操作。

*   **NF-3: 兼容性 (Compatibility):**
    *   **首选平台:** Web端应用，通过Three.js实现跨浏览器兼容，支持现代浏览器的WebGL功能。
    *   **次选平台:** 桌面端（通过Electron包装Web应用），或平板端。

*   **NF-4: 数据安全与隐私 (Data Security):**
    *   所有用户数据默认存储在本地，不上传至任何服务器，保障用户隐私。

---

### **4. 技术栈考量 (Technical Stack Considerations)**

*   **前端技术栈:**
    *   **3D引擎:** **使用Three.js**作为核心3D渲染引擎，通过WebGL实现高性能的3D场景渲染和交互
    *   **UI框架:** React + TypeScript，配合Material-UI或Ant Design组件库
    *   **状态管理:** React Three Fiber简化Three.js与React的集成
*   **后端技术栈:**
    *   **V1.0框架:** Python + FastAPI，提供高性能的异步API服务
    *   **数据库:** MySQL，提供稳定可靠的关系型数据存储
    *   **ORM:** SQLAlchemy，简化数据库操作
    *   **认证:** JWT token认证（如需多用户支持）
    *   **长期规划:** 用户量增长后，考虑迁移至Java Spring架构以支持更高并发
*   **数据存储格式:**
    *   **前端缓存:** 使用JSON格式进行本地存储和缓存
    *   **后端持久化:** 使用关系型数据库存储项目数据、实体信息和物品列表
    *   **文件存储:** 项目文件支持导入导出JSON格式

---

### **5. 未来迭代与扩展 (Future Roadmap)**

*   **V1.1 (短期):**
    *   **预设模型库:** 提供一些常见的家具模型（床、沙发、衣柜），而非只有基础几何体。
    *   **层级与分组:** 允许用户将多个实体组合（例如，将“抽屉”放入“柜子”中），形成层级关系。
    *   **物品详情:** 允许为每个物品（字符串）添加备注、图片或标签。

*   **V2.0 (中期):**
    *   **用户系统:** 实现完整的用户注册、登录和权限管理系统
    *   **云同步:** 基于FastAPI后端实现云端备份和多设备同步功能
    *   **移动端适配:** 优化Web端在移动设备上的响应式设计和触控操作
    *   **AR集成:** 利用WebXR API，通过支持AR的移动浏览器实现AR功能，将虚拟存储实体叠加在真实环境上
    *   **API开放:** 提供RESTful API接口，支持第三方应用集成

*   **V... (长期):**
    *   **共享功能:** 允许用户将自己的布局分享给家庭成员。
    *   **智能推荐:** 根据物品类型推荐存储位置。

---

这份需求文档定义了产品的核心价值、功能边界和发展方向，希望能为您的项目提供一个坚实的起点！
